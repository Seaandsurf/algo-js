let taba = [1,2,3,4,5];
let tabb = [100,101,102];

let suma = 0;
let sumb = 0;




for (let i=0; i < taba.length; i++) {
    suma += taba[i];
} let averagea = suma / taba.length;
console.log("La moyenne de tout les éléments est égal à: " + averagea);



for (let i=0; i < tabb.length; i++) {
    sumb += tabb[i]; 
} let averageb = sumb / tabb.length;
console.log("La moyenne de tout les éléments ets égal à: " + averageb);
